<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
　　<li><%= link_to task.id, task %> : : <%= task.content %> &gt; <%= task.status %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの書き込み', new_task_path %>